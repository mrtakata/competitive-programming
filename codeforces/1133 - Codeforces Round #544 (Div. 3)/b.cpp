#include <bits/stdc++.h>
using namespace std;
// define macros
#define ll long long int
const int MAXN = 200000;
const int MAXK = 100;

int n,k;
ll d[MAXN];
int main(){

    cin.tie(0);
    ios_base::sync_with_stdio(0);

    cin >> n >> k;
    for(int i = 0; i < n; i++){
        cin >> d[i];
    }

    return 0;
}